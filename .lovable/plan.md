

## Plan: Update Seaside Location Card Image

Replace the current seaside exterior image in the Locations section's "Seaside" card with the uploaded terrace community photo.

### Steps

1. **Copy uploaded image** to `src/assets/seaside-exterior.jpg` (overwrite), or save as a new file like `src/assets/terrace-community.jpg` and update the import in `LocationsSection.tsx`.
2. **Update `LocationsSection.tsx`**: Change the import and update the `alt` text to something like "Rooftop terrace community event at Innovation Campus Málaga".

This is a simple asset swap — no layout or logic changes needed.

