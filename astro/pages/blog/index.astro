---
import Layout from '../../layouts/Layout.astro';
import { PageWrapper } from '@/lib/lang-context';
import { getAlternateLinks, generateBreadcrumbJsonLd } from '@/lib/i18n';
import Blog from '@/pages/en/Blog';
const path = '/blog';
const title = 'Blog';
const description = 'Insights on coworking, remote work, and building your business from Innovation Campus.';
const alternates = getAlternateLinks(path);
const breadcrumb = generateBreadcrumbJsonLd(path, 'https://innovationcampus.biz');
---
<Layout {title} {description} {path} {alternates} {breadcrumb}>
  <PageWrapper lang="en" path={path} client:load><Blog /></PageWrapper>
</Layout>
