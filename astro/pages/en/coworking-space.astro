---
import ServicePageLayout from '../../components/ServicePageLayout.astro';
import CoworkingSpacePage from '@/pages/en/CoworkingSpace';
import serviceCoworking from '@/assets/service-coworking.webp';
import { getAlternateLinks, generateBreadcrumbJsonLd } from '@/lib/i18n';

const path = '/en/coworking-space';
const title = 'Coworking & Pricing';
const description = 'Flexible coworking plans and pricing for individuals and small teams in Málaga.';
const alternates = getAlternateLinks(path);
const breadcrumb = generateBreadcrumbJsonLd(path, 'https://innovationcampus.biz');
---

<ServicePageLayout
  {title}
  {description}
  {path}
  {alternates}
  {breadcrumb}
  subtitle="Flexible plans and pricing for individuals"
  image={serviceCoworking}
  lang="en"
>
  <CoworkingSpacePage lang="en" client:load />
</ServicePageLayout>
