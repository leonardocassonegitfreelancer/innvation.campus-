---
import ServicePageLayout from '../../components/ServicePageLayout.astro';
import BenefitsPage from '@/pages/en/Benefits';
import servicePerks from '@/assets/service-perks.webp';
import { getAlternateLinks, generateBreadcrumbJsonLd } from '@/lib/i18n';

const path = '/en/benefits';
const title = 'Member Perks';
const description = 'Exclusive discounts and offers from our partners for Innovation Campus members.';
const alternates = getAlternateLinks(path);
const breadcrumb = generateBreadcrumbJsonLd(path, 'https://innovationcampus.biz');
---

<ServicePageLayout
  {title}
  {description}
  {path}
  {alternates}
  {breadcrumb}
  subtitle="Exclusive discounts and offers for our members."
  image={servicePerks}
  lang="en"
>
  <BenefitsPage lang="en" client:load />
</ServicePageLayout>
