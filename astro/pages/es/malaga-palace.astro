---
import Layout from '../../layouts/Layout.astro';
import Navbar from '@/components/landing/Navbar';
import Footer from '../../components/Footer.astro';
import MalagaPalace from '@/pages/en/MalagaPalace';
import { getAlternateLinks, generateBreadcrumbJsonLd, generateLocalBusinessJsonLd } from '@/lib/i18n';

const path = '/es/malaga-palace';
const title = 'Málaga Palace';
const description = 'Coworking en un palacio restaurado del siglo XVIII en el centro histórico de Málaga.';
const alternates = getAlternateLinks(path);
const breadcrumb = generateBreadcrumbJsonLd(path, 'https://innovationcampus.biz');
const jsonLd = generateLocalBusinessJsonLd('malaga-palace', `https://innovationcampus.biz${path}`);
---
<Layout {title} {description} {path} {alternates} {breadcrumb} {jsonLd}>
  <div class="overflow-x-hidden">
    <Navbar lang="es" client:load />
    <MalagaPalace lang="es" client:load />
    <Footer lang="es" />
  </div>
</Layout>
