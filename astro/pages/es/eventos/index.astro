---
import Layout from '../../../layouts/Layout.astro';
import EventsPage from '@/pages/en/Events';
import { getAlternateLinks, generateBreadcrumbJsonLd } from '@/lib/i18n';

const path = '/es/eventos';
const title = 'Eventos Comunitarios';
const description = 'Únete a talleres, charlas, networking y eventos comunitarios en Innovation Campus Málaga.';
const alternates = getAlternateLinks(path);
const breadcrumb = generateBreadcrumbJsonLd(path, 'https://innovationcampus.biz');
---

<Layout {title} {description} {path} {alternates} {breadcrumb}>
  <EventsPage lang="es" client:load />
</Layout>
